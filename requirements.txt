APScheduler==3.10.4
Flask==3.0.3
requests==2.34.2
beautifulsoup4==4.12.3
# lxml ist der schnellere HTML-Parser. Auf dem Pi (aarch64) liefert pip ein
# fertiges Wheel, der Build kompiliert also nichts. Fehlt lxml doch mal, fällt
# der Code automatisch auf Pythons html.parser zurück (app/scrapers/base.py).
lxml==5.3.0
python-dotenv==1.0.1
# ICS-Kalender fuer groovestation und zentralwerk. recurring_ical_events loest
# RRULE-Wiederholungen (Zentralwerks offener Chor u.ae.) direkt fuer den
# gebrauchten Zeitraum auf, statt nur die VEVENT-Vorlagen zu liefern.
icalendar==7.3.0
recurring-ical-events==3.8.2
