aiogram==3.13.1
APScheduler==3.10.4
Flask==3.0.3
requests==2.34.2
beautifulsoup4==4.12.3
# lxml ist optional: schneller Parser, aber auf 32-bit-Pi-OS (armv7) ohne
# fertiges Wheel und dann sehr langsam zu bauen. Der Code fällt automatisch
# auf Pythons html.parser zurück, wenn lxml fehlt (siehe app/scrapers/base.py).
# Falls der Docker-Build daran hängen bleibt: diese Zeile auskommentieren.
lxml==5.3.0
python-dotenv==1.0.1
